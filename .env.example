# Server only. Existing environment values take precedence; never use VITE_ for secrets.
OPENAI_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SESSION_SECRET=
# Temporary production operation without Supabase. Default: supabase.
# Set stateless only with the Vercel firewall rule documented in docs/ai-stateless.md.
STEMCARE_STORAGE_MODE=supabase
STEMCARE_EDGE_RATE_LIMIT=false
APP_ORIGINS=https://stem-care.com,https://www.stem-care.com
TURNSTILE_SECRET_KEY=
UPSTREAM_BASE=https://apicellswhyfor.com/items
UPSTREAM_TOKEN=
RESEND_API_KEY=
CONTACT_TO=contacto@stem-care.com
# Public browser configuration for visitor-confirmed contact requests.
VITE_TURNSTILE_SITE_KEY=
# Optional local-only path to the existing agent env file (never copied).
STEMCARE_AGENT_ENV=
